#include <GAME/engine/Attachable.hpp>

/*template<class Origin>
void Attachable::attach(Origin& object) {
	assert(origin == nullptr);
	_origin = &object;
	onAttach(object);
}

void detach() {
	assert(origin != nullptr);
	onDetach();
	_origin = nullptr;
}

bool isAttached() {
	return _origin != nullptr;
}

Origin* origin() {
	return _origin;
}*/