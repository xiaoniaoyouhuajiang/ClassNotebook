#include <json2graphadapter.h>

/*class JsonAdapter: public Json2GraphAdapterInterface {
public:
    void setJsonData(QByteArray input_data) override {
        this->data = input_data;
    }
    std::map<BaseVertex*, BaseVertex*> getFlows() override {  }

    std::vector<BaseVertex*> getVertexList() override {
        auto v = new Vertex<int>();
        v->id = "cdscd";
        vector.push_back(v);
        for (;;) {

        }
    }

    VertexDataType getType() override {

    }
private:
    QByteArray data;
    std::vector<BaseVertex*> vector;
};*/
