#pragma once
#include "Ellipse.h"
#include "EqualTrapezium.h"
#include "Trapezium.h"
#include "Figure.h"

class Figure
{
public:
	//template <typename T, typename U>
	//double static compareAreas(T figure1, U figure2)
	//{
	//	if (figure1.area == figure2.area)
	//		return 3;
	//	if (figure1.area < figure2.area)
	//		return 2;
	//	if (figure1.area > figure2.area)
	//		return 1;
	//	return 0;
	//}

	//template <typename T, typename U>
	//double static compareAreas(Shape& figure1, Shape& figure2)
	//{
	//	if (figure1.area == figure2.area)
	//		return 3;
	//	if (figure1.area < figure2.area)
	//		return 2;
	//	if (figure1.area > figure2.area)
	//		return 1;
	//	return 0;
	//}
};
